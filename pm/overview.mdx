---
title: EmailDeliveryHero — Overview
description: Product charter and table of contents for EmailDeliveryHero — an open-source, localhost web app for auditing email deliverability (spam filtering, blacklists) and prescribing the exact fixes.
---

# EmailDeliveryHero — Overview

This directory holds the MDX specifications for **EmailDeliveryHero**. Every `.mdx` file in this
directory (and its subdirectories) is part of that spec set, and this `overview.mdx` is the table
of contents that links to all of them.

EmailDeliveryHero is an **open source project**. Git remote (origin):
`https://github.com/BryanStarbuck/EmailDeliveryHero.git`.

## Specifications (table of contents)

All MDX specifications in this directory and its subdirectories:

* [Overview](./overview.mdx) — this file: charter, personas, feature set, the deliverability
  checks, tech stack, ports, and the OpenAuthFederated relationship.
* [Security](./security.mdx) — the authoritative security & authentication spec: **login is
  optional** (the app is fully usable logged out as a single `default` user), how sign-in works
  through OpenAuthFederated Google Workspace SSO, the Users model (`default` vs. verified email),
  per-user settings (including `default`), the Google Cloud credentials read from
  `~/.credentials/email_delivery_hero.json`, the "Sign in" button in the account slot, and the
  backend/frontend enforcement points. Supersedes the gating described in Authentication.
* [Authentication](./authentication.mdx) — the OpenAuthFederated integration mechanics for the
  **signed-in path**: the external library (`@auth/backend` + `@auth/react`), embedded Frontend API
  middleware at `/api/v1`, Bearer-token verification, the global NestJS `JwtAuthGuard`, allowed
  domains, cookie prefix `oaf_edh`, and the out-of-repo credentials file. See Security for the
  optional-login/`default`-user model that governs when a session is actually required.
* [Engineering](./engineering.mdx) — architecture: the pnpm monorepo layout, the NestJS module
  list, the frontend structure, ports, the pluggable audit-engine design, the on-disk
  JSON/YAML store, and how periodic checks run.
* [Spam & deliverability checks](./spam_checks.mdx) — the full catalog of every health check the
  app performs, grouped into the six dashboard categories (SPF, DKIM, DMARC, Blacklists, DNS &
  Infrastructure, Spam & Content): what each sub-check measures, how it is measured, and the
  concrete remediation.
* [Report-email ingestion](./emails.mdx) — the **inbound report emails** receivers send back to a
  monitored domain: DMARC aggregate (`rua`) XML reports and SMTP TLS Reporting (TLS-RPT) JSON
  reports. How to recognize/decode/parse a `.eml`, the deliverability problems the reports reveal
  (unauthorized senders, SPF/DKIM-alignment gaps, spoofing, silent TLS/MTA-STS breakage) and their
  concrete fixes, the receive→classify→decode→parse→findings→store pipeline, the Reports UI, and the
  proactive health checks that hunt the same problems. Feeds `dmarc.real_pass_rate` and
  `infra.tls_rpt_reports_ingested`. Sample corpus: the 10 real `act3ai.com` reports under `emails/`.
* [Per-check engineering specs](./checks/overview.mdx) — the `checks/` subdirectory: one deep spec
  per check topic (SPF, DKIM, DMARC, ARC, Blacklists, MX, PTR, TLS/STARTTLS, MTA-STS, TLS-RPT,
  DANE/TLSA, DNSSEC, DNS health, domain reputation, SMTP security, BIMI, content scoring,
  List-Unsubscribe, link/URL reputation, reputation metrics, inbox placement). Each expands a slice
  of the catalog with its sub-checks, detection method, a **User Interface** section, an
  **Information Schema (database schema)** section, and when the check runs.
* [User Interface](./ui.mdx) — the consolidated UI spec: the shared design language (six locked
  categories, four status colors, the StatusCell, the brand header, the account slot), a walkthrough
  of every screen (Dashboard, Domains, Run detail, Audits, Blacklists, Settings), the two left bars,
  the common finding-presentation pattern, and a Use Cases section on how a user flows through the app.
* [Toasts & scan progress](./progress_ui.mdx) — the two "something is happening" surfaces: toasts
  re-anchored bottom-left, enlarged, and kept clear of the 256px left bar; and the live scan-progress
  dock — stacked per-domain "Running <domain>" cards with a spinning icon that drain as each domain
  finishes. Also fixes the concurrency model: parallel per-domain scans (bounded limiter) with a
  concurrency-safe persist.
* [Dashboard](./dashboard.mdx) — the main screen: the big "Email Delivery Hero" brand header, the
  TanStack table (one run per day × six color-coded category columns), the metric cell text, and
  the top-right **Run checks** button + scheduled-checks toggle/chevron.
* [Domains](./domains.mdx) — the Monitored Domains CRUD UI and, per domain, its sending IPs, DKIM
  selectors, and its own **scheduled on/off** toggle (e.g. 10 domains but only 3 on the recurring
  schedule).
* [Scheduled checks](./scheduled_checks.mdx) — recurring checks: the configuration page (frequency,
  time-of-day, multiple times per day) and the OS-appropriate scheduler for each platform (macOS
  launchd plist, Linux cron/systemd timer, Windows Task Scheduler).
* [Settings](./settings.mdx) — the Settings page: checks configuration, DNSBL zones, notifications,
  storage & data, tools & environment, and the `role:admin`-gated Admin sub-section.
* [Storage](./storage.mdx) — how state is persisted on disk (no database): the state root
  `~/T/_emaildeliveryhero/`, the hierarchical app-level `config.yaml` (settings), the
  per-logged-in-user `users/<email>/config.yaml`, the `domains.json` domain list + each domain's sync
  on/off flag, `audits.json`, the auth session store + signing secret, the two log files, the
  browser `localStorage` conventions (theme mirror, never secrets), and the store layer that fronts
  it all — identical in local and server mode.
* [Error handling & logging](./errors.mdx) — how errors and logs are handled: the two append-only
  rolling files at the storage root (`log.log` for the normal event trail, `error.err` for the fault
  trail), the dependency-free synchronous `RollingFileWriter`, the `AppLogger` that routes all of
  NestJS through the same files, the global exception filter + process handlers that guarantee
  *anything that can go wrong* lands in `error.err`, and the timestamped INFO lines for manual and
  scheduled check runs.
* Use cases:
  * [Audit a domain](./use_cases/audit_a_domain.mdx) — a user adds a domain, runs an audit, and
    sees the findings plus the exact fixes.
  * [Domain on a blacklist](./use_cases/domain_on_blacklist.mdx) — an audit finds the domain's IP
    on a DNSBL and shows the delisting remediation.
  * [View a health-check run](./use_cases/view_health_check_run.mdx) — clicking a dashboard row
    opens one run in full: the summary header and every finding grouped by the six categories.

## Key facts

The load-bearing constants for the app, stored here as the single reference point:

| Key | Value |
| --- | --- |
| **UI WebApp localhost port** | **4444** — the web app always runs on `http://localhost:4444/`. |
| Backend REST API port | `9312` (NestJS, served under `/api`; API only — not opened directly in a browser). |
| Runtime | Node >= 20, **TypeScript** (strict) throughout. |
| Repo layout | pnpm workspace monorepo under `code/` (`packages/backend`, `packages/frontend`). |
| Auth | **Login optional** — logged-out = the `default` user; sign-in is OpenAuthFederated Google Workspace SSO (no email/password). Only admin actions require a session. See [Security](./security.mdx). |
| Storage | On-disk JSON/YAML under `~/T/_emaildeliveryhero/` — no database. |
| Git remote | `https://github.com/BryanStarbuck/EmailDeliveryHero.git` |

The **UI WebApp must always run on localhost on port 4444**. This is the canonical value for the
browser-facing web app; treat it as the source of truth when wiring the frontend dev/preview
server and any launch scripts.

## Charter

EmailDeliveryHero is a **web app that runs on localhost** for **auditing email deliverability** —
determining whether a company's email sending domains are being caught by spam filters or landing
on blacklists, and telling the user how to fix them. It runs **periodic checks**, surfaces the
specific problems it finds, and prescribes the **exact remediation** to apply.

**Login is optional.** The app is fully usable logged out under a single **`default`** user; signing
in is an upgrade to a per-person identity and to admin-only actions. The only way to *sign in* is
federated Google Workspace SSO for our two company domains — see [Security](./security.mdx) (the
authoritative model) and [Authentication](./authentication.mdx) (the sign-in mechanics).

### What it does

* **Monitors your sending domains.** You register the email domains you send from (e.g.
  `whitehatengineering.com`, `act3ai.com`) and the app watches them.
* **Checks whether your email is getting caught by spam filters** — by auditing the DNS records
  that mailbox providers use to decide inbox-vs-spam (SPF, DKIM, DMARC, MX, PTR).
* **Checks whether your domains/IPs are on blacklists** — querying well-known DNS blacklists
  (DNSBLs) for the domain's sending IPs.
* **Tells you how to fix any problem it finds** — every finding carries a concrete remediation
  string ("how to fix"), not just a red/green light.
* **Runs periodic re-checks** and reports newly-introduced problems as they appear.

## Who uses it

| Persona | What they get from EmailDeliveryHero |
| --- | --- |
| **Deliverability owner / ops** | Adds the company's sending domains, runs audits, works the list of open problems and their fixes. |
| **Engineer / DNS admin** | Reads the exact record-level remediation (the SPF/DKIM/DMARC TXT to publish) and applies it at the DNS provider. |
| **Admin** | Same as above, plus admin-only settings/sections gated by the `role:admin` token claim. |

Everyone who signs in is an employee on one of the two allowed company Google Workspace domains;
there is no anonymous or external access.

## Feature set (first round)

* **Monitored Domains** — CRUD over the set of email sending domains under audit. A domain is the
  primary object in the app; everything else hangs off it.
* **Deliverability Audit** — for each domain, run a battery of checks (below) that each produce
  **findings**. Every finding has a **severity** (`ok` / `info` / `warning` / `critical`) and a
  **remediation** string. An audit rolls its findings up into a **score/status** for the domain.
* **Blacklist monitoring** — checks whether the domain's sending IPs appear on DNS blacklists and,
  when they do, shows the delisting steps. See
  [Domain on a blacklist](./use_cases/domain_on_blacklist.mdx).
* **Periodic checks** — scheduled re-audits that re-run the same checks on a cadence and surface
  any *new* problems since the last run.
* **Reporting** — a dashboard of all domains, each with its latest audit score/status and the list
  of open problems (with fixes).

## The deliverability checks performed

Each check is an independent **checker** that returns zero or more findings for a domain. First
round:

| Check | What it verifies | Typical critical finding |
| --- | --- | --- |
| **SPF** | A single, valid `v=spf1` TXT record exists, with a sane mechanism list and one `all` qualifier; no multiple SPF records; within the 10-DNS-lookup limit. | No SPF record, or two SPF records (invalid), or `+all`. |
| **DKIM** | For each known selector, a `selector._domainkey.<domain>` TXT record exists and parses as a public key (`v=DKIM1; k=rsa; p=…`). | No DKIM record found for the configured selector(s). |
| **DMARC** | A `_dmarc.<domain>` TXT record exists with `v=DMARC1` and a policy (`p=none/quarantine/reject`); reports on weak/`none` policy. | No DMARC record, or malformed policy. |
| **MX** | The domain publishes resolvable MX records with valid priorities and reachable hosts. | No MX records. |
| **Reverse DNS / PTR** | Each sending IP has a PTR record and it matches forward DNS (FCrDNS sanity). | Missing PTR, or PTR that does not resolve back to the IP. |
| **DNS Blacklist (DNSBL)** | The domain's sending IPs are not listed on well-known blocklists — Spamhaus ZEN, Barracuda, SpamCop, and similar. | IP is listed on one or more DNSBLs. |

The full engine design (each checker is a pluggable module returning findings) is in
[Engineering](./engineering.mdx).

## Tech stack (at a glance)

* **Language/runtime:** Node + **TypeScript** (strict), **pnpm** workspaces monorepo.
* **Backend:** **NestJS** REST API under `/api`, on port **9312**.
* **Frontend (UI WebApp):** **React 19 + Vite + Tailwind + shadcn/ui + lucide-react + TanStack
  Router/Query**, always on localhost port **4444** (`http://localhost:4444/`).
* **Auth:** **OpenAuthFederated**, consumed as an external open-source library (`@auth/backend` +
  `@auth/react`) — federated Google Workspace SSO, no email/password. See
  [Authentication](./authentication.mdx).
* **Storage:** first round uses on-disk **JSON/YAML files** under a state dir (default
  `~/T/_emaildeliveryhero/`) — **no database**.
* **DNS checks:** Node's native `node:dns/promises` (TXT/MX/reverse); shelling out to
  Brew-installed tools (`dig`, etc.) is noted as a future enhancement.

See [Engineering](./engineering.mdx) for the full, buildable architecture.

## Ports

| Port | Surface | Browser URL |
| --- | --- | --- |
| **4444** | Frontend / UI WebApp (React 19 + Vite) | `http://localhost:4444/` |
| **9312** | Backend (NestJS REST API under `/api`) | API only — not opened directly in a browser |

## Left navigation

The app's primary navigation is a left bar with these sections (the data source of truth is
`pm/left_bar.yaml`, written by a separate process; here we describe it in prose):

* **Dashboard** — the landing view: all domains with their latest score/status and open problems.
* **Domains** — the Monitored Domains CRUD.
* **Audits** — audit history and the findings of any given run.
* **Blacklists** — DNSBL status across all monitored domains/IPs.
* **Reports** — ingested DMARC aggregate (`rua`) and TLS-RPT report emails per domain: who sent as
  your domain, SPF/DKIM alignment, and inbound-TLS health, each with the concrete fix. See
  [Report-email ingestion](./emails.mdx).
* **Settings** — app and per-user settings.
* **Account block (bottom):** avatar / name / email → menu with **Settings**, **Admin** (gated by
  the `role:admin` claim), and **Log out**.

## Relationship to OpenAuthFederated

EmailDeliveryHero does **not** implement authentication itself. It **consumes OpenAuthFederated**
— our open-source, self-hosted identity layer (`~/BGit/Bryan_git/OpenAuthFederated/`,
`https://github.com/BryanStarbuck/OpenAuthFederated.git`) — as an external library, the same way
the reference internal app (`~/BGit/all/app/`) does. The backend uses `@auth/backend`; the
frontend uses `@auth/react`. OpenAuthFederated is brought down separately and linked in; it is
**not** copied into this repo. The full model is specified in
[Authentication](./authentication.mdx).

## Project structure

```
EmailDeliveryHero/
├── code/                    # application code (pnpm monorepo; owned by another process)
│   └── packages/
│       ├── backend/         # NestJS REST API (port 9312)
│       └── frontend/        # React 19 + Vite SPA — UI WebApp (port 4444)
├── pm/                      # product management — this directory of MDX specs
│   ├── overview.mdx         # this file
│   ├── authentication.mdx
│   ├── engineering.mdx
│   ├── spam_checks.mdx      # full catalog of every deliverability health check
│   ├── emails.mdx           # inbound report-email ingestion (DMARC aggregate + TLS-RPT)
│   ├── dashboard.mdx        # the TanStack table dashboard (6 columns, status cells)
│   ├── domains.mdx          # Monitored Domains CRUD + per-domain schedule toggle
│   ├── scheduled_checks.mdx # recurring checks: config page + per-OS scheduling
│   ├── settings.mdx         # the Settings page
│   ├── storage.mdx          # on-disk state: config.yaml, per-user config, domains.json, logs
│   ├── errors.mdx           # error handling & logging: log.log + error.err, rolling writer, filter
│   ├── left_bar.yaml        # nav source of truth (written by a separate process)
│   ├── checks/              # one deep engineering spec per check topic (21 files + index)
│   │   ├── overview.mdx     # index of the per-check specs, grouped by category
│   │   ├── spf.mdx          # SPF
│   │   ├── dkim.mdx         # DKIM
│   │   ├── dmarc.mdx        # DMARC
│   │   ├── arc.mdx          # ARC (DMARC companion)
│   │   ├── blacklists.mdx   # DNSBL / RHSBL blacklists
│   │   ├── mx_routing.mdx           # MX & mail routing
│   │   ├── reverse_dns.mdx          # PTR / FCrDNS
│   │   ├── tls_transport.mdx        # STARTTLS + MX cert health
│   │   ├── mta_sts.mdx              # MTA-STS
│   │   ├── tls_rpt.mdx              # TLS-RPT
│   │   ├── dane_tlsa.mdx            # DANE / TLSA
│   │   ├── dnssec.mdx               # DNSSEC
│   │   ├── dns_health.mdx           # NS/zone health + dangling records
│   │   ├── domain_reputation.mdx    # WHOIS/RDAP registration reputation
│   │   ├── smtp_security.mdx        # open relay / SMTP hardening
│   │   ├── bimi.mdx                 # BIMI
│   │   ├── content_scoring.mdx      # SpamAssassin content scoring
│   │   ├── list_unsubscribe.mdx     # List-Unsubscribe / one-click
│   │   ├── link_url_reputation.mdx  # URI blocklists on message links
│   │   ├── reputation_metrics.mdx   # complaint/bounce/FBL/Postmaster
│   │   └── inbox_placement.mdx      # seed-list inbox placement
│   └── use_cases/
│       ├── audit_a_domain.mdx
│       ├── domain_on_blacklist.mdx
│       └── view_health_check_run.mdx
└── CLAUDE.md                # project charter
```

<!--
  TOC maintenance: the table of contents lives at the top of this file under
  "## Specifications (table of contents)". When a new .mdx file is added anywhere under this
  directory, add a bullet there (relative ./path/to/file.mdx) with a one-line description. Keep
  subdirectory specs grouped and indented under a parent bullet.
-->
</content>
</invoke>
